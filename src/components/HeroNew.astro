---
import { Image } from 'astro:assets';
---

<section class="relative bg-[#FAF9F6] overflow-hidden">
  {/* Subtle background pattern */}
  <div class="absolute inset-0 opacity-[0.03]">
    <div class="absolute inset-0" style="background-image: radial-gradient(circle at 1px 1px, #1A1A1A 1px, transparent 0); background-size: 40px 40px;"></div>
  </div>

  <div class="container mx-auto px-6 py-16 md:py-24 relative">
    <div class="grid lg:grid-cols-2 gap-12 lg:gap-16 items-center">
      {/* Left: Content */}
      <div class="order-2 lg:order-1">
        {/* Label */}
        <div class="flex items-center gap-2 mb-6">
          <span class="w-8 h-[2px] bg-[#B8860B]"></span>
          <span class="label">Infinite Banking Concept</span>
        </div>

        {/* Main Headline */}
        <h1 class="heading-hero mb-6 text-balance">
          Banks are lending you <em class="text-[#B8860B] not-italic">your own money</em>.
        </h1>

        {/* Subheadline */}
        <p class="body-large mb-8 max-w-xl">
          Every dollar you spend on cars, vacations, and daily life — you're financing it. 
          The only question is: who keeps the profit? I'll show you how to become your own banker.
        </p>

        {/* CTA Buttons */}
        <div class="flex flex-col sm:flex-row gap-4 mb-8">
          <a href="/learn/start-here" class="btn-primary">
            Start Learning
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
            </svg>
          </a>
          <a href="https://calendly.com/brad-raschke/discovery" target="_blank" rel="noopener noreferrer" class="btn-secondary">
            Book a Discovery Call
          </a>
        </div>

        {/* Trust indicators */}
        <div class="flex flex-wrap items-center gap-6 text-sm text-[#6B6B6B]">
          <div class="flex items-center gap-2">
            <svg class="w-5 h-5 text-[#B8860B]" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
            </svg>
            <span>NNI Authorized Practitioner</span>
          </div>
          <div class="flex items-center gap-2">
            <svg class="w-5 h-5 text-[#B8860B]" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
            </svg>
            <span>9+ Years Experience</span>
          </div>
        </div>
      </div>

      {/* Right: Brad's Photo */}
      <div class="order-1 lg:order-2 flex justify-center lg:justify-end">
        <div class="relative">
          {/* Decorative elements */}
          <div class="absolute -top-4 -left-4 w-24 h-24 border-2 border-[#B8860B] rounded-lg opacity-20"></div>
          <div class="absolute -bottom-4 -right-4 w-32 h-32 bg-[#B8860B] rounded-lg opacity-10"></div>
          
          {/* Photo container */}
          <div class="relative w-72 h-72 md:w-80 md:h-80 rounded-2xl overflow-hidden shadow-2xl">
            <img 
              src="/images/brad-raschke.jpg" 
              alt="Brad Raschke - IBC Authorized Practitioner"
              class="w-full h-full object-cover"
            />
          </div>

          {/* Quote overlay */}
          <div class="absolute -bottom-6 -left-6 right-6 bg-white rounded-xl shadow-xl p-4 border-l-4 border-[#B8860B]">
            <p class="text-sm text-[#4A4A4A] italic font-serif">
              "I only teach one thing, and I teach it the way Nelson Nash intended."
            </p>
            <p class="text-xs text-[#6B6B6B] mt-2 font-medium">— Brad Raschke</p>
          </div>
        </div>
      </div>
    </div>
  </div>

  {/* Scroll indicator */}
  <div class="absolute bottom-6 left-1/2 -translate-x-1/2 hidden md:flex flex-col items-center gap-2 text-[#9A9A9A]">
    <span class="text-xs uppercase tracking-widest">Scroll to explore</span>
    <svg class="w-5 h-5 animate-bounce" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"/>
    </svg>
  </div>
</section>
