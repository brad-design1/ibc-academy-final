---
import Layout from '../../layouts/Layout.astro';
import PersonaHero from '../../components/PersonaHero.astro';
import EmailCapture from '../../components/EmailCapture.astro';

const accent = '#FF3DB8';
const rgb = '255, 61, 184';

const curatedContent = [
  {
    badge: 'Start Here',
    title: 'Where Does Your Money Actually Go?',
    description: 'Follow a paycheck from deposit to death and discover how much of your money ends up in someone else\'s bank account.',
    readTime: '8 min read',
    href: '/learn/articles/just-curious-01-where-does-money-go',
  },
  {
    badge: 'Foundation',
    title: 'The Silent Partner in Every Purchase',
    description: 'Even when you pay cash, you\'re still financing the purchase. Meet the invisible partner who profits from every dollar you spend.',
    readTime: '7 min read',
    href: '/learn/articles/just-curious-02-silent-partner',
  },
  {
    badge: 'Core',
    title: 'Why Your Savings Account Is Working Against You',
    description: 'Your savings account is supposed to help you build wealth. Instead, it\'s quietly destroying your purchasing power. Here\'s how.',
    readTime: '6 min read',
    href: '/learn/articles/just-curious-03-savings-working-against-you',
  },
  {
    badge: 'Eye-Opening',
    title: 'What Banks Actually Do With Your Money',
    description: 'Banks aren\'t safekeeping your money. They\'re using it to get rich. Here\'s exactly how the banking business really works.',
    readTime: '8 min read',
    href: '/learn/articles/just-curious-04-what-banks-actually-do',
  },
  {
    badge: 'Thought Starter',
    title: 'The Question Nobody Asks About Retirement',
    description: 'Everyone talks about saving for retirement. Nobody talks about how you\'ll access money during retirement. Here\'s the question that changes everything.',
    readTime: '7 min read',
    href: '/learn/articles/just-curious-05-question-about-retirement',
  },
  {
    badge: 'The Big Idea',
    title: 'What If You Were the Bank?',
    description: 'Everything you\'ve learned points to one question: What if you could capture banking profits instead of paying them? Here\'s how.',
    readTime: '9 min read',
    href: '/learn/articles/just-curious-06-what-if-you-were-bank',
  },
];

const basics = [
  {
    icon: '💡',
    title: 'It\'s a Process, Not a Product',
    description: 'IBC is a strategy for how you manage money — specifically, how you finance the things you buy. The product (whole life insurance) is just the tool.',
  },
  {
    icon: '🏦',
    title: 'You Become Your Own Banker',
    description: 'Instead of borrowing from banks and paying them interest, you build a system that lets you borrow from yourself — and pay yourself back.',
  },
  {
    icon: '📖',
    title: 'It Was Created by Nelson Nash',
    description: 'A forester from Alabama who spent decades studying how money actually works. His book "Becoming Your Own Banker" started the movement.',
  },
  {
    icon: '🔑',
    title: 'It\'s About Control',
    description: 'Not returns. Not beating the market. Control over your capital, your access to money, and the banking function that every person performs.',
  },
];
---

<Layout title="The Curious Path — Discover What IBC Is All About" description="New to Infinite Banking? Start here. Plain-English explanations, no jargon, no sales pitch. Just the concept explained clearly.">

  <!-- Hero -->
  <PersonaHero
    badge="The Curious Path"
    headline="You Keep Hearing About It. Here's What It Actually Is."
    highlightWord="What It Actually Is."
    subhead="No jargon. No sales pitch. No pressure. Just a clear explanation of what the Infinite Banking Concept is, how it works, and why people are talking about it."
    primaryCta={{ text: 'Start With the Basics', href: '#curriculum' }}
    secondaryCta={{ text: 'Back to Home', href: '/' }}
    accentHex={accent}
  />

  <!-- The Basics -->
  <section class="py-20 lg:py-28 relative">
    <div class="absolute inset-0 bg-gradient-to-b from-navy-900 to-navy-dark/50"></div>
    <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center max-w-2xl mx-auto mb-16 fade-in-up">
        <span style={`color: ${accent};`} class="text-sm font-semibold tracking-[0.2em] uppercase">The Basics</span>
        <h2 class="mt-4 text-3xl sm:text-4xl font-extrabold text-white tracking-tight">
          What You Need to <span style={`color: ${accent};`}>Know First</span>
        </h2>
        <p class="mt-4 text-lg text-slate-400">
          Before diving into the details, here are the four things that matter most.
        </p>
      </div>

      <div class="grid sm:grid-cols-2 gap-6 lg:gap-8 stagger-children">
        {basics.map((item) => (
          <div class="glass-card rounded-2xl p-6 lg:p-8">
            <span class="text-3xl mb-4 block">{item.icon}</span>
            <h3 class="text-xl font-bold text-white mb-2">{item.title}</h3>
            <p class="text-slate-400 leading-relaxed">{item.description}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- How It Works (Simple) -->
  <section class="py-16 lg:py-20 relative">
    <div class="absolute inset-0 bg-navy-dark/30"></div>
    <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="max-w-3xl mx-auto text-center fade-in-up">
        <div class="glass-surface rounded-2xl p-8 lg:p-12">
          <h3 class="text-2xl font-bold text-white mb-4">How It Works (Simply)</h3>
          <div class="space-y-6 text-left">
            <div class="flex gap-4 items-start">
              <div class="w-10 h-10 rounded-full flex items-center justify-center font-bold text-sm flex-shrink-0" style={`background: rgba(${rgb}, 0.2); color: ${accent};`}>1</div>
              <div>
                <h4 class="text-white font-semibold">You Fund a Specially Designed Whole Life Policy</h4>
                <p class="text-slate-400 text-sm mt-1">Not a regular policy — one engineered to maximize cash value growth through paid-up additions.</p>
              </div>
            </div>
            <div class="flex gap-4 items-start">
              <div class="w-10 h-10 rounded-full flex items-center justify-center font-bold text-sm flex-shrink-0" style={`background: rgba(${rgb}, 0.2); color: ${accent};`}>2</div>
              <div>
                <h4 class="text-white font-semibold">Your Cash Value Grows Guaranteed + Dividends</h4>
                <p class="text-slate-400 text-sm mt-1">Every year, your money grows. No market risk. No volatility. Guaranteed growth plus dividends from a mutual company.</p>
              </div>
            </div>
            <div class="flex gap-4 items-start">
              <div class="w-10 h-10 rounded-full flex items-center justify-center font-bold text-sm flex-shrink-0" style={`background: rgba(${rgb}, 0.2); color: ${accent};`}>3</div>
              <div>
                <h4 class="text-white font-semibold">You Borrow Against It When You Need Capital</h4>
                <p class="text-slate-400 text-sm mt-1">Need money for a car, a home, a business? Take a policy loan. No application. No credit check. Your money keeps growing.</p>
              </div>
            </div>
            <div class="flex gap-4 items-start">
              <div class="w-10 h-10 rounded-full flex items-center justify-center font-bold text-sm flex-shrink-0" style={`background: rgba(${rgb}, 0.2); color: ${accent};`}>4</div>
              <div>
                <h4 class="text-white font-semibold">You Pay Yourself Back — With Interest</h4>
                <p class="text-slate-400 text-sm mt-1">The interest you'd normally pay a bank? It goes back into your system. Every cycle makes the next one more powerful.</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Curated Content Track -->
  <section id="curriculum" class="py-20 lg:py-28 relative">
    <div class="absolute inset-0 bg-gradient-to-b from-navy-dark/30 to-navy-900"></div>
    <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center max-w-2xl mx-auto mb-16 fade-in-up">
        <span style={`color: ${accent};`} class="text-sm font-semibold tracking-[0.2em] uppercase">Your Curriculum</span>
        <h2 class="mt-4 text-3xl sm:text-4xl font-extrabold text-white tracking-tight">
          The Beginner's <span style={`color: ${accent};`}>Track</span>
        </h2>
        <p class="mt-4 text-lg text-slate-400">
          Start at the top. Each article builds on the last. Take your time.
        </p>
      </div>

      <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-5 stagger-children">
        {curatedContent.map((item, index) => (
          <a href={item.href} class="group glass-card rounded-2xl p-6 block hover-accent-card">
            <div class="flex items-center gap-3 mb-4">
              <span class="w-8 h-8 rounded-full flex items-center justify-center text-xs font-bold flex-shrink-0" style={`background: rgba(${rgb}, 0.2); color: ${accent};`}>{index + 1}</span>
              <span class="text-xs font-bold px-2.5 py-1 rounded-md uppercase tracking-wider" style={`color: ${accent}; background: rgba(${rgb}, 0.1);`}>{item.badge}</span>
            </div>
            <h3 class="text-lg font-bold text-white transition-colors duration-300 mb-2 leading-snug" style={`--hover-color: ${accent};`}>
              {item.title}
            </h3>
            <p class="text-slate-400 text-sm leading-relaxed mb-4">{item.description}</p>
            <div class="flex items-center justify-between pt-4 border-t border-white/[0.06]">
              <span class="text-slate-500 text-xs font-medium">{item.readTime}</span>
              <svg class="w-5 h-5 text-slate-600 group-hover:translate-x-1 transition-all duration-300" style={`--hover-color: ${accent};`} fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
              </svg>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="py-16 lg:py-20 relative">
    <div class="absolute inset-0" style={`background: linear-gradient(to bottom right, rgba(${rgb}, 0.1), rgb(10, 22, 40));`}></div>
    <div class="relative max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 text-center fade-in-up">
      <h2 class="text-3xl sm:text-4xl font-extrabold text-white tracking-tight">
        Curiosity Is the <span style={`color: ${accent};`}>First Step</span>
      </h2>
      <p class="mt-4 text-lg text-slate-400">
        You don't need to commit to anything. Just start reading. The concept speaks for itself.
      </p>
      <div class="mt-8 flex flex-col sm:flex-row gap-4 justify-center">
        <a
          href="/learn/start-here"
          class="inline-flex items-center justify-center px-8 py-4 text-navy-900 font-extrabold text-base rounded-xl transition-all duration-300 shadow-lg hover:-translate-y-0.5 min-h-[48px]"
          style={`background: linear-gradient(to right, ${accent}, ${accent}dd); box-shadow: 0 10px 15px -3px rgba(${rgb}, 0.25);`}
        >
          Start Reading — It's Free
          <svg class="ml-2 w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M13 7l5 5m0 0l-5 5m5-5H6" />
          </svg>
        </a>
        <a
          href="/"
          class="inline-flex items-center justify-center px-8 py-4 border border-slate-600/50 text-white font-semibold text-base rounded-xl transition-all duration-300 min-h-[48px] path-secondary-cta"
          style={`--hover-color: ${accent}; --hover-border: rgba(${rgb}, 0.4);`}
        >
          Explore Other Paths
        </a>
      </div>
    </div>
  </section>

  <!-- Email Capture -->
  <EmailCapture />
</Layout>

<style>
  .path-secondary-cta:hover {
    color: var(--hover-color);
    border-color: var(--hover-border);
  }
  .hover-accent-card h3 { transition: color 0.3s; }
  .hover-accent-card:hover h3 { color: var(--hover-color) !important; }
  .hover-accent-card:hover svg { color: var(--hover-color) !important; }
</style>
